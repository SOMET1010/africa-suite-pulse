# Environment Variables Documentation
# Copy this file to .env and fill in the actual values

# ============================================================================
# DEVELOPMENT ENVIRONMENT VARIABLES
# ============================================================================

# Supabase Configuration (Required for all environments)
# Project URL from your Supabase dashboard
VITE_SUPABASE_URL=https://your-project.supabase.co
# Public anon key from Supabase API settings  
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# ============================================================================
# PRODUCTION ENVIRONMENT VARIABLES  
# ============================================================================

# Database Configuration (Production only)
# Direct database URL for server-side operations
SUPABASE_DB_URL=postgresql://user:pass@host:port/db

# Service Role Key (Production only)
# Service role key with admin privileges - NEVER expose to client
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ============================================================================
# OPTIONAL ENVIRONMENT VARIABLES
# ============================================================================

# Analytics & Monitoring (Optional)
# Add analytics tracking ID if using external analytics
VITE_ANALYTICS_ID=your_analytics_id

# Feature Flags (Optional)
# Enable/disable specific features in development
VITE_ENABLE_DEBUG_MODE=false
VITE_ENABLE_DEVTOOLS=true

# API Rate Limiting (Optional)
# Configure API request limits
VITE_API_RATE_LIMIT=100

# ============================================================================
# ENVIRONMENT VARIABLE VALIDATION
# ============================================================================

# Required for all environments:
# - VITE_SUPABASE_URL
# - VITE_SUPABASE_ANON_KEY

# Required for production:
# - SUPABASE_DB_URL  
# - SUPABASE_SERVICE_ROLE_KEY

# Optional but recommended:
# - VITE_ANALYTICS_ID (for tracking)
# - VITE_ENABLE_DEBUG_MODE (for debugging)

# Security Notes:
# - Never commit actual keys to version control
# - Use different keys for dev/staging/production
# - Rotate keys regularly
# - Monitor key usage in Supabase dashboard